profiles {
  omics {
    docker.enabled = true
    process.container = params.container
    // Generate helpful reports; Omics will upload them with results
    timeline { enabled = true; file = "${params.outdir}/timeline.html" }
    report   { enabled = true; file = "${params.outdir}/report.html"   }
    trace    { enabled = true; file = "${params.outdir}/trace.txt"     }
    dag      { enabled = true; file = "${params.outdir}/dag.dot"       }
  }
}

params {
  alphabet = 'ACGTN'
  log_freq = 1000
  outdir   = 'results'   // **local**; do NOT use s3:// here—Omics collects this folder
}
